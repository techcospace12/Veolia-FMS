# Copy this file to .env and fill in the values for local development.
DATABASE_URL="file:./dev.db"

# Optional: enable HTTP Basic Auth gate. Leave BOTH unset locally to disable
# and land straight on the app. Set on Render to protect the shared URL.
# BASIC_AUTH_USER="veolia"
# BASIC_AUTH_PASSWORD="fms2026"
